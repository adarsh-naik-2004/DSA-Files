class Solution {
public:

    // THE REASON player 1 always wins is because they can always take the first move and the game is structured such that they can always counter the second player's moves.

    bool stoneGame(vector<int>& piles) {
        return true;
    }
};